---
type: post
title: React 教程
---


Context 上下文对象，全局共享数据






React Redux

```
npm install react-redux --save
```


```
<Provider store={store} />
</Provider>



redux-observable
redux-observable是基于RxJS 6的Redux中间件，用来异步请求
```
npm install --save redux-observable
```

Epic

```
import { ofType } from 'redux-observable';
const loginEpic = (action$, state$) => action$.pipe(
  ofType('entry/login'),
  delay(1000),
  mapTo({ type: 'PONG' }),
)
```
